import { StickyNote } from "lucide-react"
import { PlaceholderPage } from "@/components/placeholder-page"

export default function NotesPage() {
  return (
    <PlaceholderPage 
      title="Notes" 
      description="Capture important thoughts and meetings" 
      icon={StickyNote} 
    />
  )
}
